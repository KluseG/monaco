export interface ColorConfig {
  error?: {
    background: string;
    foreground: string;
  };
  info: {
    background: string;
    foreground: string;
  };
  notice?: {
    background: string;
    foreground: string;
  };
  success?: {
    background: string;
    foreground: string;
  };
  warn?: {
    background: string;
    foreground: string;
  };
}

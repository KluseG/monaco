export type Verbosity = number;

/**
 * @export
 * @enum {number}
 */
export enum BackgroundColors {
  debug = '#007bff',
  error = '#dc3545',
  info = '#6c757d',
  notice = '#17a2b8',
  primary = '#343a40',
  success = '#28a745',
  warn = '#ffc107',
}

/**
 * @export
 * @enum {number}
 */
export enum ForegroundColors {
  debug = '#ffffff',
  error = '#ffffff',
  info = '#ffffff',
  notice = '#ffffff',
  primary = '#ffffff',
  success = '#ffffff',
  warn = '#ffffff',
}
